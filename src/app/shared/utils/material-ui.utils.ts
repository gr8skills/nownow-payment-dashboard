import { MatSnackBarConfig } from "@angular/material/snack-bar";

export const MatSnackbarConfig = {
  duration: 6000,
  horizontalPosition: 'center',
  verticalPosition: 'bottom',
} as MatSnackBarConfig;